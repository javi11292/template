{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "commit": "git add -A && git commit -m update && git push heroku master",
    "start": "pm2 start index.js",
    "test": "pm2 kill && pm2 start index.js --watch && pm2 monit"
  },
  "dependencies": {
    "express": "^4.16.4",
    "pm2": "^3.2.4"
  }
}