{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "docker-compose up --build",
    "sh": "docker-compose exec server sh"
  },
  "dependencies": {
    "compression": "^1.7.0",
    "express": "^4.17.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0"
  }
}
