{
    "name": "server",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "commit": "git add -A && git commit -m",
        "push": "git push heroku master",
        "upload": "npm run commit --",
        "postupload": "npm run push",
        "start": "pm2-runtime start ecosystem.config.js --env production",
        "test": "pm2 start ecosystem.config.js --watch"
    },
    "dependencies": {
        "express": "^4.16.4",
        "pm2": "^3.2.4"
    }
}