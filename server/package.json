{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/docker build",
    "ssh": "node scripts/docker ssh",
    "start": "node scripts/docker start"
  },
  "dependencies": {
    "compression": "^1.7.0",
    "express": "^4.17.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0"
  }
}